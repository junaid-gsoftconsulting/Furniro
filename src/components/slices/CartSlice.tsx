import { createSlice } from "@reduxjs/toolkit";

const initialState = {
    
};

const CartSlice = createSlice({
  name: "cart",
  initialState,
  reducers: {},
});

export default CartSlice.actions;
